# URL of the PC running gui/central/main.py
SYNC_BASE_URL=http://192.168.1.100:8100

# Must match central SYNC_TOKEN
SYNC_TOKEN=gml-sync-2026

# Optional. Defaults to computer hostname if left blank in launcher.
NODE_ID=MY-PC-NAME
